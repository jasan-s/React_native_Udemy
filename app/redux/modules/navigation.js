// import { Root } from '~/config/routing'

// const JUMP_TAB = 'JUMP_TAB'

// const navReducer = (state, action) => {
//   const newState = Root.router.getStateForAction(action, state)
//   const navState = newState ? newState : state
//   switch (action.type) {
//     default :
//       return navState
//   }
// }
// export default navReducer

